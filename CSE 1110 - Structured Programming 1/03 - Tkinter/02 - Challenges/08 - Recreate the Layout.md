# Challenge - Recreate the Layout
The object of this challenge is to practice adding elements and layouts using Tkinter.

For these "Recreate the Layout" challenges, I am not so concerned with an exact, pixel for pixel recreation. However, there will be required inclusions listed under each image.

## Mild 🌶️
Recreate the following layout:

![alt text](./Images/08%20-%20Mild.png)

Be sure to include:
* The window title
* A frame with a color
* 1 label for input that has a text color and a background color
* 1 entry
* A button that is centered at the bottom of the window
* The label shares a row with the entry
* There is padding around each widget to create space

## Medium 🌶️🌶️
Recreate the following layout:

![alt text](./Images/08%20-%20Medium.png)

Be sure to include:
* The window title
* A frame with a color
* 2 labels for input that have background and text colors
* 2 entries
* 1 checkbutton that is centered
* A button that is centered at the bottom of the window
* There is padding around each widget to create space

## Spicy 🌶️🌶️🌶️
Recreate the following layout:

![alt text](./Images/08%20-%20Spicy.png)

Be sure to include:
* The window title
* A frame with a color
* 1 heading label that has a different font size, text color, and background
* 2 labels for input that have background and text colors
* 2 entries
* A frame with 2 radiobuttons that share a row that is centered
* A button that is centered at the bottom of the window
* There is padding around each widget to create space