# Challenge - Recreate the Layout
The object of this challenge is to practice adding elements and layouts using Tkinter.

For these "Recreate the Layout" challenges, I am not so concerned with an exact, pixel for pixel recreation. However, there will be required inclusions listed under each image.

## Mild 🌶️
Recreate the following layout:

![alt text](./Images/03%20-%20Mild.png)

Be sure to include:
* The window title
* A red frame
* An orange frame inside the red frame that fills the space

## Medium 🌶️🌶️
Recreate the following layout:

![alt text](./Images/03%20-%20Medium.png)

Be sure to include:
* The window title
* A red frame
* An orange frame inside the red frame that fills the space
* A yellow frame inside the orange frame that fills the space
* A green frame inside the yellow frame that fills the space
* A cyan frame inside the green frame that fills the space

## Spicy 🌶️🌶️🌶️
Recreate the following layout:

![alt text](./Images/03%20-%20Spicy.png)

Be sure to include:
* The window title
* A red frame
* An orange frame inside the red frame that fills the space
* A yellow frame inside the orange frame that fills the space
* A green frame inside the yellow frame that fills the space
* A cyan frame inside the green frame that fills the space
* A blue frame inside the cyan frame that fills the space
* A purple frame inside the blue frame that fills the space
* A magenta frame inside the purple frame that fills the space